Shop app from the Angular tutorial (https://angular.io/start).

- ~npm install -g @angular/cli~
- cd path/to/repo
- ~npm install~
- ~ng serve~ (by default on port 4200. To specifiy use the port flag:
  ~--port 4201~)
